types:
  definitions:
    default-package: com.palantir.alias
    objects:
      Foo:
        fields:
          myField: Bar

      Bar:
        alias: map<string, Baz>

      Baz:
        union:
          str: string
          date: datetime